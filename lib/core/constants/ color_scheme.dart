import 'package:flutter/material.dart';

// Dark Theme Colors
const Color darkBG = Color(0xFF111111); // Background color
const Color darkPrimary = Color(0xFF44AEFE); // Primary color (light blue)
const Color darkOnPrimary = Color(0xFF000000); // Text/icons on primary (black)
const Color darkSecondary = Color(0xFF222222); // Secondary color (dark teal)
const Color darkOnSecondary = Colors.white; // Text/icons on secondary (white)
const Color darkTertiary = Color(0xFF4D4D4D); // Tertiary color (Mine Shaft)
const Color darkOnTertiary =
    Color(0xFF9E9E9E); // Text/icons on tertiary (black)
const Color darkError = Color(0xFFCF6679); // Error color (red)
const Color darkOnError = Color(0xFF000000); // Text/icons on error (black)
const Color darkSurface = Color(0xFF333333); // Surface color (dark grey)
const Color darkOnSurface = Colors.white; // Text/icons on surface (white)
const Color darkOutline = Color(0xFF757575); // Outlines/dividers (muted grey)
const Color darkInverseSurface = Colors.white; // Inverse surface (white)
const Color darkOnInverseSurface =
    Color(0xFF121212); // Text/icons on inverse surface (black)
const Color darkSurfaceTint = Color(0xFF90CAF9); // Surface tint (light blue)

// Light Theme Colors
const Color lightBG = Colors.white; // Background color
const Color lightPrimary = Color(0xFF008FFE); // Primary color (blue)
const Color lightOnPrimary = Color(0xFFDDF1F9); // Text/icons on primary (white)
const Color lightSecondary =
    Color(0xFFF8F8F8); // Secondary color (light blue accent)
const Color lightOnSecondary =
    Color(0xFF0D47A1); // Text/icons on secondary (blue)
const Color lightTertiary = Color(0xFFF3F3F3); // Tertiary color (greyish teal)
const Color lightOnTertiary =
    Color(0xFF9E9E9E); // Text/icons on tertiary (white)
const Color lightError = Color(0xFFCF6679); // Error color (dark red)
const Color lightOnError = Colors.white; // Text/icons on error (white)
const Color lightSurface = Colors.white; // Surface color (white)
const Color lightOnSurface = Colors.black; // Text/icons on surface (black)
const Color lightOutline = Color(0xFFBDBDBD); // Outlines/dividers (light grey)
const Color lightInverseSurface =
    Color(0xFF2C2C2C); // Inverse surface (dark grey)
const Color lightOnInverseSurface =
    Colors.white; // Text/icons on inverse surface (white)
const Color lightSurfaceTint = Color(0xFF0D47A1); // Surface tint (blue)
